[flake8]
max-line-length = 80
#Ignored because of black
ignore = E203, W503

exclude = '''
          (
            /(
                \.eggs         # exclude a few common directories in the
              | \.git          # root of the project
              | \.hg
              | \.mypy_cache
              | \.tox
              | \.venv
              | venv
              | _build
              | buck-out
              | build
              | dist
              | node_modules
              | .serverless
              | .vscode
            )/
          )
          '''
